# This is a basic workflow
name: CI

on:
  push:
    branches: 
    - main

jobs:
  
  my-testing:
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v1
      
      - name: List current folder
        run: ls -la